extends ../layouts/application.pug

block header
  =t('views.labels.newLabel.header')

block content
  +formFor()(action=route('labelsCreate') method='post')
    +input(label, 'name', errors)(type="text")
    +submit(label)(value=t('views.labels.newLabel.submit'))