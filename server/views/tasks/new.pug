extends ../layouts/application.pug

block header
  = t('views.tasks.new.signUp')

block content
  +formFor()(action=route('createTask') method='post')
    +input(task, 'name', errors)(type="text")
    +textarea(task, 'description', errors)(rows="3")
    +select(task, 'statusId', errors, users)
    +select(task, 'executorId', errors, statuses)
    +submit(task)(value=t('views.tasks.new.submit'))